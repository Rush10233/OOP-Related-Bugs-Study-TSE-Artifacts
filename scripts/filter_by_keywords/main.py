from resolver import ReportTextParser
from calculate import *
#import sys
# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    '''
    worklist_gcc, worklist_llvm = extract_worklist()
    
    for work in worklist_llvm:
        parser = ReportTextParser(work, "llvm")        
        parser.launch()
    for work in worklist_gcc:
        parser = ReportTextParser(work, "gcc")        
        parser.launch()
    '''
    calculate()


