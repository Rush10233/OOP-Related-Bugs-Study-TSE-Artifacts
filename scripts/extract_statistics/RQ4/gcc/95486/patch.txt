{
    	      remove_constraints (fprime);
    	      set_constraints (fprime, ci);
    	    }

if (err)
    	    continue;

    	    continue;

    	    continue;

{
    	      remove_constraints (fprime);
    	      set_constraints (fprime, ci);
    	    }

if (err)
    	    continue;

    	    continue;

    	    continue;

