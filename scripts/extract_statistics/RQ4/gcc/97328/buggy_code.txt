&& TREE_CODE (index) != FIELD_DECL)

        new_ctx.object = build_ctor_subob_ref (index, type, ctx->object);

&& TREE_CODE (index) != FIELD_DECL)

        new_ctx.object = build_ctor_subob_ref (index, type, ctx->object);

