fns = lookup_arg_dependent (fnname, fns, *args);

fns = lookup_arg_dependent (fnname, fns, *args);

fns = lookup_name (fnname, LOOK_where::NAMESPACE);
      fns = lookup_arg_dependent (fnname, fns, *args);

fns = lookup_arg_dependent (fnname, fns, *args);

